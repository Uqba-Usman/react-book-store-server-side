#mainform.container.col-sm-12
  #signupbox.mainbox.col-md-6.col-md-offset-3.col-sm-8.col-sm-offset-2(style=' margin-top:50px')
    .panel.panel-info
      .panel-heading
        .panel-title Upload Image
      .panel-body
        form.form-horizontal(role='form', method='post', action='/api/v1/upload', enctype='multipart/form-data')
          if (message.length > 0)
            div(class="alert alert-"+status+" col-sm-12 "+message)
          .form-group
            label.col-md-3.control-label(for='image') Profile Image
            .col-sm-9
              input.form-control(type='file', name='profile', accept='')
          .form-group
            // Button
            .col-md-offset-3.col-md-9
              button#btn-signup.btn.btn-info(type='submit')
                i.icon-hand-right
                |    Upload
